#Notas:
Este es mi primer programa en node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Lista la tabla de multiplicar por pantalla
                                           [boolean] [required] [default: false]
  -h, --hasta    Indica hasta que numero multiplicar      [number] [default: 10]

  RECORDAD EJECUTAR NPM INSTALL PARA INSTALAR LOS PAQUETES NECESARIOS!!!
```